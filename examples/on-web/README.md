# [**`trpc-browser`**](../../README.md) (on-web)

### Getting started

Make sure your current working directory is at `/trpc-browser` root.

```bash
npm install
npm run build
cd examples/on-web
npm run dev
```
